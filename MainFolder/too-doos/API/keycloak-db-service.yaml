apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.36.0 (ae2a39403)
  labels:
    io.kompose.service: keycloak-db
  name: keycloak-db
spec:
  ports:
    - name: "5432"
      port: 5432           # <-- CLUSTER port
      targetPort: 5432     # kontenerowy port
  selector:
    io.kompose.service: keycloak-db
